// This is the component to render a jsonb field
// There will need to be a way to add/remove fields to the jsonb object
// 1. get fields list for this table
// 2. loop through fields
// 3. build input depending on field properties
export const Jsonb = memo(({ table, data, onChange }) => {
  return null
})
